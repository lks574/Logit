# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# 진행 상황 / 재개

작업 재개 시 `docs/PROGRESS.md`를 먼저 읽을 것 (현재 상태·실행법·완료/미완·다음 후보).
상세: `docs/FOUNDATION.md`(DS/네비 계약) · `docs/DATA_MODEL.md`(데이터 모델) · `docs/STORE.md`(스토어 계약) · `git log`.
