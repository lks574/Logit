rules_version = '2';

// 클라우드 백업: 사용자는 자기 문서(backups/{uid})만 읽고 쓸 수 있다.
service cloud.firestore {
  match /databases/{database}/documents {
    match /backups/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
